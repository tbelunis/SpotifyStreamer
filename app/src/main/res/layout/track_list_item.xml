<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="?android:listPreferredItemHeight">

    <ImageView
        android:id="@+id/track_list_item_imageview"
        android:layout_width="@dimen/image_width"
        android:layout_height="@dimen/image_height"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"/>

    <TextView
        android:id="@+id/track_list_item_album_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/track_list_item_imageview"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="20dp"
        android:textSize="10sp"
        android:text="Album"/>

    <TextView
        android:id="@+id/track_list_item_track_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/track_list_item_imageview"
        android:layout_below="@id/track_list_item_album_name"
        android:layout_marginLeft="10dp"
        android:textSize="10sp"
        android:text="Track"/>

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/holo_gray_light"/>

</RelativeLayout>